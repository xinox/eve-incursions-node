services:
  init:
    image: node:22-alpine
    working_dir: /eve-incursions
    volumes:
      - ./:/eve-incursions
    command: npm i

  server:
    command: sh -c "/eve-incursions/node_modules/.bin/nodemon -w src --ext ts --exec '/eve-incursions/node_modules/.bin/ts-node --swc src/index.ts'"
    depends_on:
      init:
        condition: service_completed_successfully
    ports:
    - 4001:4001
    labels:
      caddy: localhost
      caddy.reverse_proxy: /api* {{upstreams 4001}}
      caddy.@cachedFiles.path: "*.jpg *.jpeg *.png *.gif *.ico *.svg"
      caddy.header: '@cachedFiles Cache-Control "public, max-age=604800, must-revalidate"'

  scheduler:
    command: sh -c "/eve-incursions/node_modules/.bin/ts-node --swc src/scheduler.ts"
    depends_on:
      init:
        condition: service_completed_successfully

  ws:
    command: sh -c "npm start"
    depends_on:
      init:
        condition: service_completed_successfully
    ports:
      - 4003:4003
    labels:
      caddy: localhost
      caddy.reverse_proxy: /ws {{upstreams 4003}}

  frontend:
    command: sh -c "npm run dev"
    depends_on:
      init:
        condition: service_completed_successfully
    ports:
    - 4002:3000
    labels:
      caddy: localhost
      caddy.reverse_proxy: /* {{upstreams 3000}}
      caddy.@cachedFiles.path: "*.jpg *.jpeg *.png *.gif *.ico *.svg"
      caddy.header: '@cachedFiles Cache-Control "public, max-age=604800, must-revalidate"'
