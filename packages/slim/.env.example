# Optional. Defaults to packages/slim/data/app.db when commands are run from packages/slim.
DB_MODE=local
DB_FILE=data/app.db

# Set this when DB_MODE=supabase.
DATABASE_URL=
DATABASE_SSL=require
DATABASE_POOL_MAX=1
DB_SYNCHRONIZE=false

# The slim package scripts default to port 4004.
PORT=4004
